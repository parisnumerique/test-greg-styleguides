mixin video(data)
  .component.component-video
    .video-wrapper
      if data.placeholder
        .video-placeholder!= data.placeholder
      script.video-embed(type="text/html")
        if block
          != block
        else if data.block
          != data.block
