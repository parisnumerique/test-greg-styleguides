include ../../modules/button/button

mixin buttons(data)
  .component.component-buttons
    if data.title
      .buttons-title= data.title
    ul.buttons-items
      each item in data.items
        li
          +button(item)
