mixin postit(data)
  .component.component-postit(class=data ? data.modifiers : [])
    if data.title
      h2.anchor#postit= data.title
    if block
      block
    else if data.block
      != data.block
    else
      p postit
