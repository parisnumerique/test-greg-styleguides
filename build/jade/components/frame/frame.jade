mixin frame(data)
  .component.component-frame(class=data ? data.modifiers : [])
    fieldset
      if data.title
        legend= data.title
      div
        if block
          block
        else if data.content
          != data.content
