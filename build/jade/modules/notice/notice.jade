mixin notice(opts)
  .notice(class=opts.modifiers id=opts.id)
    if opts.closable
      .close
    if block
      p.notice-content
        block
    else if opts.block
      p.notice-content
        != opts.block
    else
      p.notice-content notice
