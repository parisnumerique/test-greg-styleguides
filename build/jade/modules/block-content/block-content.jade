mixin block-content(data)
  - attributes = data.attributes || {}
  .block-content(class=data.modifiers)&attributes(attributes)
    h2.block-content-title= data.title
    .block-content-content
      if block
        block
      else if data.block
        != data.block
      else
        | blue block
