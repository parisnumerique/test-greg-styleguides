mixin block-aside(data)
  .block-aside(class=data.modifiers)
    if data.title
      h3.block-aside-title(id=data.title_id)= data.title
    .block-aside-content
      if block
        block
      else if data.block
        != data.block
      else
        | content
