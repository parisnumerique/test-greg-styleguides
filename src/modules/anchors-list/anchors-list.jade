mixin anchors-list(data)
  - data = data || {}
  .anchors-list&attributes(data.attributes || [])
    if data && data.items
      each item in data.items
        .anchor-bar(class=item.modifiers)
          a.anchor-link(href=item.href)= item.text
          span.anchor-progress
