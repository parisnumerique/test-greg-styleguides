mixin button(opts)
  - modifiers = opts.modifiers || []
  if opts.href
    if opts.icon
      - modifiers.push("icon")
    a.button(href=opts.href title=opts.title target=opts.target class=modifiers)
      if modifiers && modifiers.indexOf("marker") != -1
        i.icon.icon-marker
      else if opts.icon
        i.icon(class="icon-#{opts.icon}")
      = opts.text
  else
    button.button(class=modifiers)
      = opts.text
