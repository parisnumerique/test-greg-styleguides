mixin block-content(data)
  - var attributes = data.attributes || {}
  .block-content(class=data.modifiers)&attributes(attributes)
    h3.block-content-title= data.title
    .block-content-content
      if block
        block
      else if data.block
        != data.block
      else
        | blue block
