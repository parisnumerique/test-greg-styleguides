mixin tags(opts)
  ul.tags
    each tag, index in opts
      li
        if tag.href
          a.tags-item(href="#{tag.href}")= tag.text
        else
          span.tags-item= tag.text
        if index < opts.length-1
          span.tags-separator —
