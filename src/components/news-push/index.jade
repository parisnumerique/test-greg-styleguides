include news-push
include ../../helpers/code

.lsg-component(style="padding:50px")
  +news-push(defaults)
+code({language: "jade"}).
  include news-push

  +news-push(#{JSON.stringify(defaults, null, 2)})
