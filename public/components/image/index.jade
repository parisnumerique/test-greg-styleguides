include image
include ../../helpers/code

h2 Petite image
+image(defaults)
+code({language: "jade"}).
  include image

  +image(#{JSON.stringify(defaults)})

h2 Grande image
+image(large_image)
+code({language: "jade"}).
  include image

  +image(#{JSON.stringify(large_image)})

h2 Image verticale
+image(vertical_image)
+code({language: "jade"}).
  include image

  +image(#{JSON.stringify(vertical_image)})
