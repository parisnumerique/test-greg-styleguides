include ../heading/heading

mixin get-involved(opts)
  .get-involved
    +heading(opts.heading)
    .get-involved-content
      - each item in opts.items
        a.get-involved-card(href=item.href style='background-image: url(#{item.image});')
          p.get-involved-text= item.text

